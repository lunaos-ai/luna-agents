---
name: browser-test
displayName: Browser Test (shortcut)
description: "Shortcut: Full browser testing with screenshots and auto-fix -> /ll-browser-test"
version: 1.0.0
category: testing
shortcut_for: ll-browser-test
---

# /browser-test — Full Browser Testing

Shortcut for `/ll-browser-test`. Launch app, test every flow, screenshot, auto-fix.

```
/browser-test http://localhost:3000
/browser-test http://localhost:3000 auth
/browser-test http://localhost:3000 full
```
