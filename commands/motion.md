---
name: motion
displayName: Motion System (shortcut)
description: "Shortcut: Production motion design system — Framer + GSAP + Lottie + Rive, typed, reduced-motion safe -> /ll-motion"
version: 1.0.0
category: design
shortcut_for: ll-motion
---

# /motion — Motion Design System

Shortcut for `/ll-motion`. Consolidates all motion behind a typed primitive layer with spring presets, choreography hooks, page transitions, and reduced-motion guards.

```
/motion                          # install full system
/motion --action audit           # scan existing project
/motion --action refactor        # consolidate ad-hoc animations
```
