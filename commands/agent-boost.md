---
name: agent-boost
displayName: Agent Boost (shortcut)
description: "Shortcut: WASM-based simple task executor -> /ll-agent-boost"
version: 1.0.0
category: optimization
shortcut_for: ll-agent-boost
---

# /agent-boost — Agent Boost

Shortcut for `/ll-agent-boost`. WASM-based deterministic code transforms — sub-millisecond execution, zero token cost.

